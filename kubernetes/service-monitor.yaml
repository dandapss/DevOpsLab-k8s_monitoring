apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: k8s-monitoring
  namespace: monitoring
  labels:
    release: monitoring
spec:
  namespaceSelector:
    matchNames:
      - minilab
  selector:
    matchLabels:
      app: k8s-monitoring
  endpoints:
    - port: http
      path: /metrics
      interval: 15s
